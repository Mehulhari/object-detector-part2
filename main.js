Status=""

function setup(){
   canvas= createCanvas(900,800);
 
      canvas.position(300,100);
}
function preload(){
Image="ppppp.jpeg"
}
function gotResult(){
   
   objects[i].width;
   objects[i].height;
   (objects[i].x , objects[i].y);
   text(objects[i].label);
   object[i].confidence;
}
 
